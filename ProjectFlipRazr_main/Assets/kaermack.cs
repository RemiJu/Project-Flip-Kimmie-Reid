using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class kaermack : MonoBehaviour
{

    class PictureData
    {
        /*string playerFileLocation
        enum gameLocation;
        enum[] pictureStuff;*/
    }

    //a database of all photos taken, to get images out of the RAM and on to the drive

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
